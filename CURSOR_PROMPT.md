# Cursorにそのまま貼る完全プロンプト

あなたは、企業ブランディングとフロントエンド実装に強いシニアWebデザイナー兼フロントエンドエンジニアです。
株式会社STAYCLOSEの新公式サイトを、このリポジトリ内の既存ファイルをベースに完成させてください。

## 最重要：デザインの正解データ
`design-reference/MASTER-approved-top.png` が正式に承認されたマスターデザインです。
**この画像をデザインのSource of Truthとして扱ってください。**
勝手に別のテイストへ再設計しないでください。

現状の `index.html` / `styles.css` / `script.js` は、マスターデザインを実装するためのベースです。
修正時は、まずマスター画像とブラウザ表示を比較し、差分を小さくする方向で調整してください。

## ブランドコンセプト
株式会社STAYCLOSEは「外からアドバイスする会社」ではなく、現場に入り、一緒に汗をかいて成果まで伴走する実働型パートナーです。
キーメッセージは以下です。

**一緒に汗をかいて、その一歩先へ。**

AI企業のような人工的・近未来的なデザインにはしないでください。
人間味、誠実さ、清潔感、青空、前進、余白、上質さを重視します。
参考サイトの方向性は https://hypex.jp/ ですが、コピーはせず、余白・写真の使い方・タイポグラフィの上質さだけを参考にします。

## 絶対に変更しないもの
1. ロゴは `assets/logo.png` を使う。
2. HEROのコピーは「一緒に汗をかいて、その一歩先へ。」
3. HERO見出しは上品な明朝体。Zen Old Mincho / 游明朝系を優先。
4. HEROは大きく、青空写真を画面いっぱいに使う。
5. HEROのPC用画像は `assets/hero-01-wide.jpg` と `assets/hero-02-wide.jpg` の2枚（モバイルでは元画像 `hero-01.jpg` / `hero-02.jpg`）を約10秒ごとにゆっくりクロスフェード。
6. `hero-01.jpg` のKanata・ココ・ポコはSTAYCLOSEの世界観を表す重要要素なので消さない。
7. 事業内容の順番は必ず SALES → WEB → AI。
8. SALESには Customer Success を含める。
9. WEBには SEO / MEO / AIO を必ず明記。
10. WordPressは使わない。

## ヘッダー
左：正式ロゴ
右：
- 私たちについて
- サービス
- 実績
- 私たちの強み
- 会社概要
- お知らせ
- お問い合わせCTA

PCではHERO上に透明で重ねる。
スクロール後だけ白背景＋軽いbackdrop blurにする。
モバイルはハンバーガー。

## HEROの目標
`MASTER-approved-top.png` にできるだけ忠実にする。
- 大きな青空
- 左側は白から透明へ抜ける自然なグラデーション
- 人物・Kanata・ココ・ポコが画像の中心〜右側に見える
- 左側に明朝体キャッチコピー
- 下に説明文
- BUSINESS DRIVE PARTNER
- 左下に 01 — 02 のスライダー表示

注意：背景を白くしすぎて画像全体が薄くならないようにする。
コピー周辺だけ可読性を確保し、右側の空・人物・キャラクターは鮮やかに残す。

## SERVICE
3枚横並び（モバイル1列）。
各カードは写真を全面に使用し、濃紺の薄いオーバーレイをかける。
カードを「AIっぽいUIカード」にしすぎない。
写真・文字・余白で魅せる。

### 01 SALES / SUPPORT / CUSTOMER SUCCESS
インサイドセールスからカスタマーサポート、カスタマーサクセスまで一気通貫で支援。

### 02 WEB / DIGITAL
制作して終わりではなく、検索・地図・AI検索から成果につながるWEBを育てる。
SEO / MEO / AIO のタグを表示。

### 03 AI / DX / SYSTEM
業務効率化、AI導入、DX、社内システム・業務ツール開発。
「AIっぽさ」を前面に出すのではなく、業務を前に進める手段として見せる。

## 下層セクションのデザイン原則
- 背景は白とごく薄いグレーを交互にする
- 角丸を乱用しない
- グラデーションを乱用しない
- アイコンを大量に並べない
- 余白を広く取る
- 日本語見出しは明朝、本文は読みやすいゴシック
- 英字の小見出しはManrope系で字間を広く
- 「AIが作ったサイト」に見えるテンプレ的な表現を避ける

## 実装品質
- HTML5の意味的な構造
- WCAGを意識したalt/aria
- CSSカスタムプロパティを使用
- PC / tablet / mobile対応
- CLSを抑える
- 画像は遅延読み込み（HERO以外）
- prefers-reduced-motion対応
- JavaScriptは必要最小限
- 外部JSライブラリは禁止

## 作業ルール
1. 既存ファイルを読んでから編集する。
2. `design-reference/MASTER-approved-top.png` を必ず参照する。
3. 変更後はローカルサーバーで表示確認する。
4. 1440px幅と390px幅で崩れを確認する。
5. HERO、ヘッダー、SERVICEはマスターとの差分を最優先で修正する。
6. 文字が画像上で読みにくい場合、画像自体を暗くするのではなく局所グラデーションで解決する。
7. 正式ロゴを別ロゴに置き換えない。
8. コピーを勝手に変更しない。
9. デザイン変更をする場合は、変更理由が明確なものだけにする。

## 最終目標
「小さな会社のWordPressサイト」ではなく、
**営業・WEB・AI/DXを横断して、本気で現場に入り伴走する会社**として信頼される、上質なコーポレートサイトにする。
ただし派手さより、余白・写真・言葉・タイポグラフィで魅せる。

まずはトップページを完成度95%以上まで仕上げ、マスター画像と見比べても違和感のない状態にしてください。
